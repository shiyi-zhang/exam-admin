spring:
  application:
    name: ssp-app-web
  datasource:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost:3306/test?characterEncoding=utf8&useSSL=false
      username: root
      password: 123456
server:
  port: 9000
# 打印sql
logging:
  level:
    com.exam.yiyou.examadmin.repository.mapper: DEBUG
pagehelper:
  helperDialect: mysql
  reasonable: true
